group "com.tny.game"

apply plugin: "maven"

dependencies {

    compile(
            project(":tny-game-net"),
            "org.jmock:jmock-junit4:${vers.jmock}",
            "junit:junit:${vers.junit}",
            libs.'mockito',
    )

}