group "com.tny.game"

apply plugin: "maven"

dependencies {

    testImplementation project(":tny-game-cache-test")

    api "junit:junit:${vers.junit}"
    api "org.jmock:jmock-junit4:${vers.jmock}"
    api project(":tny-game-cache")

}